var x = 1;

var y;

var firstname = 'Kevin';

var lastName: string = 'Huang';

var num1 = 100;

var num2 : number = 2;

function addNumbers(n1: number,n2: number,n3: number){
    var res = n1 + n2 + n3;
    var msg = 'Sum is ' + res;
    alert(msg);
}   

var init:(s: string, p: string, c: string) => void 
    = function(start, pause, clean){

}


addNumbers(num1,num2,7);

init('s', 'p', 'c111');